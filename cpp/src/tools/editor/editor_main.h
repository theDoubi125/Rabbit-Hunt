#pragma once

namespace editor
{
	void drawEditor(float deltaTime);

	extern bool demoWindowSelected;
	extern bool pArraySelected;
	extern bool aArraySelected;
	extern bool testSelected;
	extern bool idleSelected;
	extern bool moveSelected;
	extern bool dispatcherSelected;
	extern bool pathfindSelected;
}		   
